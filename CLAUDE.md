# Maintenance notes for Claude Code

## README changelog

Keep the **three most recent versions** in the `## Changelog` section of `README.md`. When releasing a new version, prepend a new entry and remove the oldest one so the list stays at three. Each entry should include the version number as a sub-heading and 1–4 bullets describing user-visible changes (not internal refactors).
